---
box: wercker/python
services:
    - mies/rethinkdb
build:
    steps:
        - virtualenv
        - pip-install:
            packages_list: pytest
        - script:
            name: Run tests
            code: py.test tests/
